#include <stdio.h>
/**
  * main - Entry Point
  *
  * Description - a program that prints the alphabet in lowercase
  *
  * Return: always 0 (success)
  */
int main(void)
{
	int l = 97;
	for (l = 97; l <= 122; l++){
		putchar(l);
	}
	return (0);
}

